import PropTypes from 'prop-types';

const Bookmark = () => {
    return (
        <div>
            
        </div>
    );
};
Bookmark.propTypes = {
    bookmark: PropTypes.object
}
export default Bookmark;